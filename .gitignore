node_modules/
dist/
coverage/
.env
storage/

# Backups: private keys, downloaded copies and restored key files never go to git
backup-keys/
*.uzzbak
restored-keys*.env
*.pem

# Local preview config for the desktop app
.claude/

# Connection string of a remote database for ops scripts (never committed)
.env.render
.env.*.local
