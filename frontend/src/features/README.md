# Feature modules

Keep business-specific UI, hooks, schemas, and query definitions together in a
feature folder. For example: `features/bookings/` or `features/contracts/`.

Shared, reusable presentation components belong in `src/components`; shared
framework-independent helpers belong in `src/lib`.
